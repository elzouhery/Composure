services:

  first:
    image: alpine:3.20


  second:
    image: alpine:3.20
    volumes:
        - ./data:/data
        - cache:/cache

  third:
    image: alpine:3.20
    environment:
    - FLAT_LIST_INDENT=1
    - ANOTHER=2

volumes:

  cache:

    driver: local
