FROM --platform=linux/amd64 golang:1.24-alpine AS builder  # pinned for reproducibility
WORKDIR /src
RUN apk add --no-cache git \
    # this comment sits inside the continuation
    && go build -o /app ./cmd/server \
    && chmod +x /app
ENV PATH=/usr/local/bin:$PATH
EXPOSE 8080
