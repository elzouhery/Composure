# An existing .env, with a comment, a blank line and an inline comment on a
# value — every one of them a byte this operation must not touch.
COMPOSE_PROJECT_NAME=demo

VPN_SERVER_URL=your-domain.dyndns.com # free examples http://duckdns.org
API_TOKEN=t0ken
