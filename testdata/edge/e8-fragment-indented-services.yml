# A fragment: indented service definitions with no top-level key, concatenated
# into a real compose file by a build step. Parses as YAML, is not a project.
# Grafana's devenv generates dozens of these.
  alert_webhook_listener:
    build: docker/blocks/alert_webhook_listener
    ports:
      - "3010:3010"
